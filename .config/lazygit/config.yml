git:
  diffRenderers:
    - colorArg: always
      command: delta-lazygit-pager
customCommands:
  - key: "C"
    context: "files"
    command: "quill --yes"
    description: "Commit with AI-generated message"
    output: log
    loadingText: "Committing..."
